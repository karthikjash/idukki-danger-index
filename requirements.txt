# Core runtime packages actually imported by the project.
# Heavy geospatial libraries (geopandas, rasterio, netCDF4) are NOT imported
# anywhere yet and are listed as optional extras below.
#
# The ML engine (ml/) intentionally uses ONLY numpy + pandas: it trains an
# LSTM, ridge baseline and logistic hazard classifier in pure NumPy so the
# whole suite runs offline with no heavyweight ML stack. Swap in
# scikit-learn / TensorFlow later by editing ml/models.py - nothing else
# needs to change.

# Data handling
pandas>=2.0.0
numpy>=1.24.0

# Data access
requests>=2.31.0

# Mapping
folium>=0.14.0

# Backend API + dashboard server
fastapi>=0.100.0
uvicorn>=0.25.0
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0

# Seasonal risk report generation (PDF)
fpdf2>=2.7.0

# --- Optional extras (uncomment when the corresponding feature lands) ---
# # Geospatial & raster processing (DEM slope, inundation, boundaries)
# geopandas>=0.13.0
# shapely>=2.0.0
# rasterio>=1.3.0
# netCDF4>=1.6.0
#
# # Heavier ML stack (replacement for the pure-NumPy engine in ml/)
# scikit-learn>=1.3.0
# scipy>=1.10.0
# tensorflow>=2.13.0
